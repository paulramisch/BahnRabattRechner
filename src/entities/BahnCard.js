class BahnCards {
    constructor(id, name, ageGroup, ticketClass, discountPercentage, price, discountAmount) {
      this.id = id;
      this.name = name;
      this.ageGroup = ageGroup;
      this.ticketClass = ticketClass;
      this.discountPercentage = discountPercentage;
      this.price = price;
      this.discountAmount = discountAmount;
    }
  }